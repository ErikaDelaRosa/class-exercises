// first we build the function
// then we add exports. in from of the function 

//exports.sum = (a, b)=> a + b;
//exports.sub = (a, b)=> a - b;
//exports.mul = (a, b)=> a * b;
//exports.divi = (a, b)=> a / b;

/* module.exports =  {
    sum = (a, b) => a + b,
    sub = (a, b) => a - b,
    mul = (a, b) => a * b,
    divi = (a, b )=> a / b,
}; 
*/
//or as alternative 

/* 
module.exports = {
    sum,
    sub,
    multi,
    divi,
};
*/